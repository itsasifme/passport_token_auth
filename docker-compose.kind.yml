services:
  app:
    container_name: app
    build:
      args:
        user: asif
        uid: 1412
        env_file: .env.kind
      context: .
      dockerfile: /docker/php-fpm/Dockerfile
      target: server_deployment_php
    image: passport_token_auth-app:latest
    volumes:
      - app_supervisor_logs:/var/www/storage/logs/supervisor
    expose:
      - "9000"
    environment:
      - APP_ENV=production
      # .env.kind leaves these blank on purpose (see Dockerfile comment) — real values
      # are supplied here, mirroring what the k8s Secret provides for the kind overlay.
      - DB_PASSWORD=root
      - APP_KEY=base64:gUOOhq/odpN93S5S7/+4w0xPvDkuvh2TTI+7QTPn+jI=
      # Only compose sets this — k8s migrates once via its own initContainer instead.
      - RUN_MIGRATIONS_ON_BOOT=true
    working_dir: /var/www/
    depends_on:
      mysql:
        condition: service_healthy
    restart: unless-stopped

  nginx:
      container_name: nginx
      build:
        context: .
        dockerfile: ./docker/nginx/Dockerfile
        target: server_deployment_nginx
      environment:
        - NGINX_HOST=app
        - NGINX_PORT=9000
      ports:
        - "8000:80"
      depends_on:
        - app  
      restart: unless-stopped

  mysql:
      image: mysql:8.0
      container_name: mysql
      environment:
        MYSQL_ROOT_PASSWORD: root
        MYSQL_DATABASE: passport_token_auth
        MYSQL_USER: asif
        MYSQL_PASSWORD: asif
      ports:
        - "3307:3306"
      volumes:
        - mysql-data:/var/lib/mysql
      healthcheck:
        test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-uasif", "-pasif"]
        interval: 5s
        timeout: 5s
        retries: 10
        start_period: 30s
      restart: unless-stopped

volumes:
    mysql-data:
    app_supervisor_logs: