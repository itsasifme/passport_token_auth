apiVersion: v1
kind: Secret
metadata:
  labels:
    app: app
  name: app-secret
type: Opaque
stringData:
  # PLACEHOLDER: generate with `php artisan key:generate --show` against the production .env.
  APP_KEY: "CHANGE_ME"
  # PLACEHOLDER: real production DB root/user password.
  DB_PASSWORD: "CHANGE_ME"
  # PLACEHOLDER: real production mail credentials.
  MAIL_USERNAME: "CHANGE_ME"
  MAIL_PASSWORD: "CHANGE_ME"
