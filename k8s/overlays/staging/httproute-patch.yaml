apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: nginx
spec:
  # PLACEHOLDER: replace with the real staging hostname (must match app-env-configmap.yaml's APP_URL).
  hostnames:
    - staging.example.com
